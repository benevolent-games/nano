
import {suite, test} from "@e280/science"

export default suite({
	"test": test(async() => {
		// const figs = {flower: new Art("flower.001", 1)}
		// const studio = new Studio()
		// const flower = studio.lease(figs.flower)
		// flower.position = new Vec3(1, 2, 3)
		// const assets = mockAssets(figs)
		// studio.use(assets)
	}),
})

